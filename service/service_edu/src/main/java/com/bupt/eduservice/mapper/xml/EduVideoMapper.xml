<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bupt.eduservice.mapper.EduVideoMapper">
    <insert id="insertEduVideo" parameterType="com.bupt.eduservice.entity.EduVideo">
        insert into edu_video(course_id,chapter_id,title,video_source_id,video_original_name,sort,play_count
        is_free,duration,status,size,version,gmt_create,gmt_modified)
        values(#{course_id},#{chapter_id},#{title},#{video_source_id},#{video_original_name},#{sort},#{play_count},
        #{is_free},#{duration},#{status},#{size},#{version},#{gmt_create},#{gmt_modified})
    </insert>
</mapper>
